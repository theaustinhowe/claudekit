Run TypeScript type-checking for a single workspace.

The user will provide the app or package name: $ARGUMENTS

Run `pnpm --filter <name> typecheck` and report any type errors found. If no errors, confirm the workspace is clean.
