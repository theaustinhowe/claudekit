// Seed script — currently a no-op since Inside bootstraps from GitHub data.
// Run with: pnpm --filter inside seed

async function main() {
  console.log("Inside: no seed data needed — connect a repo via Settings to get started.");
}

main().catch(console.error);
