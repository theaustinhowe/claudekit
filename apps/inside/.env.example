# Default directory for new projects (default: ~/Projects)
# @hint Leave empty to use the default location
# NEXT_PUBLIC_DEFAULT_DIRECTORY=

# Database path override (default: ~/.inside/data.duckdb)
# @hint Leave empty to use the default location
# DATABASE_PATH=
