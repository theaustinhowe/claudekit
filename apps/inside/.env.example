# GitHub Personal Access Token (required)
# @url https://github.com/settings/tokens
# Needs "repo" scope for private repos, or just public_repo for public repos
GITHUB_TOKEN=ghp_...

# Database path override (default: ~/.inside/data.duckdb)
# DATABASE_PATH=/custom/path/to/data.duckdb
