import type { Metadata } from "next";
import type { PropsWithChildren } from "react";

export const metadata: Metadata = { title: "Workspaces" };

export default function WorktreesLayout({ children }: PropsWithChildren) {
  return children;
}
