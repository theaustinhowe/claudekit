// Seed script — currently a no-op since Inspector bootstraps from GitHub data.
// Run with: pnpm --filter inspector seed

async function main() {
  console.log("Inspector: no seed data needed — connect a repo via Settings to get started.");
}

main().catch(console.error);
